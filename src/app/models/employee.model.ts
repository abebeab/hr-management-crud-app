export interface Employee {
  id: number;
  name: string;
  departmentId: number;
  salaryId: number;
  companyId: number;
  // Optional: add email, joinDate, etc.
  // email?: string;
  // joinDate?: Date;
}